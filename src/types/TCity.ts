export type TCity = {
  city: string;
  country: string;
  coords: {
    longitude: number;
    latitude: number;
  };
};
export type TApiCity = {
  name: string;
  lat: number;
  lon: number;
  country: string;
};
